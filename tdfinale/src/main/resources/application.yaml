spring:
  application:
    name: tdfinale
  datasource:
    url: jdbc:h2:mem:tdfinaledb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
